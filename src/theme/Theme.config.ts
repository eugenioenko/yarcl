import type { ThemeConfig } from "./Theme.types";

export const defaultThemeConfig: ThemeConfig = {
  base: {
    size: "md",
    radius: "md",
    shadow: "none",
  },
  button: {
    variant: "solid",
    color: "primary",
    size: "md",
    radius: "md",
    shadow: "none",
  },
};
