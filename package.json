{
  "name": "yarcl-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm -C consumer dev",
    "build": "pnpm -C consumer build && pnpm -C consumer-b build",
    "typecheck": "tsc -p library && tsc -p consumer && tsc -p consumer-b",
    "dev:b": "pnpm -C consumer-b dev",
    "test:e2e": "node e2e/run.mjs",
    "docs:api": "typedoc"
  },
  "devDependencies": {
    "playwright-core": "^1.63.0",
    "typedoc": "^0.28.20",
    "typescript": "^5.9.0",
    "vite": "^7.3.6"
  }
}
